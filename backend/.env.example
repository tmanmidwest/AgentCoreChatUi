# ─── Agent Configuration ───────────────────────────────────────────────────────
# The full ARN of your AWS Bedrock AgentCore runtime agent
AGENT_ARN=arn:aws:bedrock-agentcore:us-east-1:279667771168:runtime/saviynt_support_agent-yHVxA2Bioa

# The endpoint name to invoke. Shown in the Endpoints tab of your agent in the console.
# Defaults to "DEFAULT" — only change this if you created a custom endpoint name.
AGENT_ENDPOINT_NAME=DEFAULT

# AWS region where your AgentCore runtime is deployed (parsed from ARN if not set)
AWS_REGION_AGENT=us-east-1

# ─── AWS Credentials ────────────────────────────────────────────────────────────
# Option A: IAM Role (recommended for ECS/EC2 — leave these blank)
# Option B: Access keys (for local dev or Vercel/Railway)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=   # only needed for temporary credentials

# ─── App Settings ───────────────────────────────────────────────────────────────
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-me-to-a-long-random-string

# URL of your frontend (for CORS). Use * to allow all origins (not recommended for prod).
FRONTEND_URL=http://localhost:5173

# Port for the backend server
PORT=3001

# ─── Registration ───────────────────────────────────────────────────────────────
# Set to "true" to allow anyone to register. "false" = first-run only, then admin-only.
ALLOW_REGISTRATION=false

# ─── Database ───────────────────────────────────────────────────────────────────
# SQLite file path (relative to backend directory). For Railway/ECS, use a mounted volume.
# DB_PATH=./data/chat.db
